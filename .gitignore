# OS
.DS_Store

# Dependencies
node_modules/

# Environment / Local config
.env
.env.local

# Bun / Build output
*.log
dist/

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/
